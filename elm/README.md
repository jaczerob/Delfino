# Event Login Manager (ELM)

ELM is the login server for Delfino. It handles user authentication, session management, and routing players to the
appropriate channel servers.

This uses an event-based architecture to handle the events (packets) sent by the clients.